# Auth / HTTP
msal==1.20.0
requests==2.31.0
PyJWT==2.8.0

# Time / Data
pytz==2024.2
pandas==2.2.3

# MySQL driver (pick ONE family; this matches `import mysql.connector`)
mysql-connector-python==8.4.0

pymysql 

# HTML parsing
beautifulsoup4==4.12.3
# (Optional) Faster parser for bs4: lxml==4.9.4

# Windows COM (for win32com.client)
pywin32
tqdm
pyodbc
msal
pyspark 
semantic-link
